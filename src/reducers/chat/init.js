const initData = {
    // input: "",
    recent: "",
    response: "",
    allowChat: false,
    allowLoading: false,
    allowAnimation: false, 
    // allow animation for appearing each word in the response
}

export default initData;